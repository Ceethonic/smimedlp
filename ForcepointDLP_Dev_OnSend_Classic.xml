<?xml version="1.0" encoding="utf-8"?>
<OfficeApp
  xmlns="http://schemas.microsoft.com/office/appforoffice/1.1"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:bt="http://schemas.microsoft.com/office/officeappbasictypes/1.0"
  xmlns:mailappor="http://schemas.microsoft.com/office/mailappversionoverrides"
  xsi:type="MailApp">

  <!-- DEV: możesz dać osobny GUID, żeby nie kolidować z PROD -->
  <Id>11111111-2222-3333-4444-555555555555</Id>

  <Version>1.0.0.0</Version>
  <ProviderName>Forcepoint DLP (DEV)</ProviderName>
  <DefaultLocale>pl-pl</DefaultLocale>

  <DisplayName DefaultValue="Forcepoint DLP (DEV Classic OnSend)" />
  <Description DefaultValue="DEV: Classic Outlook (outlook.exe) ItemSend + Diagnostics." />

  <IconUrl DefaultValue="https://ADDIN_BASE_URL/icons/icon32.png" />
  <HighResolutionIconUrl DefaultValue="https://ADDIN_BASE_URL/icons/icon80.png" />
  <SupportUrl DefaultValue="https://ADDIN_BASE_URL/diagnostics.html" />

  <AppDomains>
    <AppDomain>https://ADDIN_BASE_URL</AppDomain>
  </AppDomains>

  <Hosts>
    <Host Name="Mailbox" />
  </Hosts>

  <!-- Exchange 2019 on-prem: trzymaj się <= 1.5; on-send działa mimo że "oficjalnie" 1.8, bo support matrix jest szerszy. :contentReference[oaicite:2]{index=2} -->
  <Requirements>
    <Sets DefaultMinVersion="1.5">
      <Set Name="Mailbox" />
    </Sets>
  </Requirements>

  <FormSettings>
    <Form xsi:type="ItemEdit">
      <DesktopSettings>
        <SourceLocation DefaultValue="https://ADDIN_BASE_URL/index.html?env=dev" />
      </DesktopSettings>
    </Form>
  </FormSettings>

  <Permissions>ReadWriteMailbox</Permissions>

  <Rule xsi:type="RuleCollection" Mode="Or">
    <Rule xsi:type="ItemIs" ItemType="Message" FormType="Edit" />
    <Rule xsi:type="ItemIs" ItemType="Appointment" FormType="Edit" />
  </Rule>

  <VersionOverrides xmlns="http://schemas.microsoft.com/office/mailappversionoverrides" xsi:type="VersionOverridesV1_0">
    <VersionOverrides xmlns="http://schemas.microsoft.com/office/mailappversionoverrides/1.1" xsi:type="VersionOverridesV1_1">
      <Requirements>
        <bt:Sets DefaultMinVersion="1.5">
          <bt:Set Name="Mailbox" />
        </bt:Sets>
      </Requirements>

      <Hosts>
        <Host xsi:type="MailHost">
          <DesktopFormFactor>

            <!-- Function file dla ItemSend -->
            <FunctionFile resid="residFunctionFileUrl" />

            <!-- 1) ON-SEND (ItemSend) -->
            <ExtensionPoint xsi:type="Events">
              <Event Type="ItemSend"
                     FunctionExecution="synchronous"
                     FunctionName="onMessageSendHandler" />
            </ExtensionPoint>

            <!-- 2) Panel diagnostyczny (przycisk w oknie tworzenia maila) -->
            <ExtensionPoint xsi:type="MessageComposeCommandSurface">
              <OfficeTab id="TabDefault">
                <Group id="grpDev">
                  <Label resid="residGroupLabel"/>
                  <Control xsi:type="Button" id="btnDiagnostics">
                    <Label resid="residBtnLabel"/>
                    <Supertip>
                      <Title resid="residBtnLabel"/>
                      <Description resid="residBtnDesc"/>
                    </Supertip>
                    <Icon>
                      <bt:Image size="16" resid="residIcon16"/>
                      <bt:Image size="32" resid="residIcon32"/>
                      <bt:Image size="80" resid="residIcon80"/>
                    </Icon>
                    <Action xsi:type="ShowTaskpane">
                      <SourceLocation resid="residDiagnosticsUrl"/>
                    </Action>
                  </Control>
                </Group>
              </OfficeTab>
            </ExtensionPoint>

          </DesktopFormFactor>
        </Host>
      </Hosts>

      <Resources>
        <bt:Images>
          <bt:Image id="residIcon16" DefaultValue="https://ADDIN_BASE_URL/icons/icon16.png"/>
          <bt:Image id="residIcon32" DefaultValue="https://ADDIN_BASE_URL/icons/icon32.png"/>
          <bt:Image id="residIcon80" DefaultValue="https://ADDIN_BASE_URL/icons/icon80.png"/>
        </bt:Images>

        <bt:Urls>
          <bt:Url id="residFunctionFileUrl" DefaultValue="https://ADDIN_BASE_URL/index.html?env=dev" />
          <bt:Url id="residDiagnosticsUrl" DefaultValue="https://ADDIN_BASE_URL/diagnostics.html?env=dev" />
        </bt:Urls>

        <bt:ShortStrings>
          <bt:String id="residGroupLabel" DefaultValue="Forcepoint DEV"/>
          <bt:String id="residBtnLabel" DefaultValue="DLP Logs"/>
        </bt:ShortStrings>

        <bt:LongStrings>
          <bt:String id="residBtnDesc" DefaultValue="Otwórz panel diagnostyczny i podgląd logów dodatku (DEV)."/>
        </bt:LongStrings>
      </Resources>

    </VersionOverrides>
  </VersionOverrides>
</OfficeApp>
